# 调试与风险

## 常见失败类型

### 资源路径错误

PCK 覆盖时路径不一致是最常见问题。

### DLL 版本不兼容

游戏更新或依赖变化后，逻辑补丁可能直接失效。

### GDExtension 缺失

涉及 FMOD、Spine、Sentry 时，缺失扩展会导致场景或脚本加载异常。

### Spine 版本不兼容

当前导出的大量 `skel` 是 4.2.x，使用 3.8 编辑器会报 `EOFException`。