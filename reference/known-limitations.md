# 已知限制

## 当前恢复结果不能等价替代官方工程

原因包括：

- 原始 `res://src/.../*.cs` 没有完整保留
- 一些 addon C# 脚本路径缺失
- 部分 `.gdextension` 依赖缺失或无法按开发态恢复
- 发布态导入资源不等价于作者态资源

## 代码层限制

当前 `analysis/decompiled_cs/sts2:1` 是结构化骨架，不是完整高保真反编译源码。